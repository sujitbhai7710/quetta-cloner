name: Test2

on:
  workflow_dispatch:
    inputs:
      only:
        description: "Optional comma-separated subset of names from names.txt (empty = all)"
        required: false
        default: ""
      make_release:
        description: "Also attach every .apk as a separate, raw (non-zip) asset to a GitHub Release"
        type: boolean
        default: true
  workflow_call:
    inputs:
      only:
        description: "Optional comma-separated subset of names from names.txt (empty = all)"
        type: string
        default: ""
      make_release:
        description: "Also attach every .apk as a separate, raw (non-zip) asset to a GitHub Release"
        type: boolean
        default: true

permissions:
  contents: write

env:
  APK_SOURCE_TAG: apk-source
  APK_SOURCE_NAME: Quetta.apk
  KS_PASS: android
  KS_ALIAS: clonekey
  KEY_PASS: android

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      names: ${{ steps.names.outputs.names }}
    steps:
      - uses: actions/checkout@v4

      - name: Build clone-name matrix from names.txt
        id: names
        env:
          ONLY: ${{ inputs.only }}
        run: |
          set -euo pipefail
          if [ -n "$ONLY" ]; then
            names=$(printf '%s' "$ONLY" | tr ',' '\n' | sed 's/^ *//;s/ *$//' | grep -v '^$')
          else
            names=$(grep -v '^[[:space:]]*$' names.txt)
          fi
          json=$(printf '%s\n' "$names" | jq -R . | jq -s -c .)
          echo "names=$json" >> "$GITHUB_OUTPUT"
          echo "Clone matrix: $json"

      - name: Echo matrix
        run: echo "matrix is ${{ steps.names.outputs.names }}"

      - name: Download source APK
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release download "$APK_SOURCE_TAG" --repo "$GITHUB_REPOSITORY" \
             --pattern "$APK_SOURCE_NAME" --output "$APK_SOURCE_NAME" --clobber
          ls -la "$APK_SOURCE_NAME"

      - name: Signing keystore (reuse CLONE_KEYSTORE_B64 secret if set, else generate)
        env:
          CLONE_KEYSTORE_B64: ${{ secrets.CLONE_KEYSTORE_B64 }}
        run: |
          set -euo pipefail
          mkdir -p keystore
          if [ -n "$CLONE_KEYSTORE_B64" ]; then
            printf '%s' "$CLONE_KEYSTORE_B64" | base64 -d > keystore/clone.keystore
            echo "Using keystore from CLONE_KEYSTORE_B64 secret"
          else
            keytool -genkeypair -v \
              -keystore keystore/clone.keystore -storetype PKCS12 \
              -alias "$KS_ALIAS" -keyalg RSA -keysize 2048 -validity 10000 \
              -storepass "$KS_PASS" -keypass "$KEY_PASS" \
              -dname "CN=APK Clone, OU=Clone, O=Clone, C=US" > /dev/null
          fi

      - name: Share keystore with build jobs
        uses: actions/upload-artifact@v4
        with:
          name: keystore
          path: keystore/clone.keystore
          retention-days: 1

      - name: Share source APK with build jobs
        uses: actions/upload-artifact@v4
        with:
          name: source-apk
          path: Quetta.apk
          retention-days: 1
          compression-level: 0

  build:
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        name: ${{ fromJSON(needs.prepare.outputs.names) }}
    name: clone ${{ matrix.name }}
    steps:
      - uses: actions/checkout@v4

      - name: Show name
        run: echo "building clone ${{ matrix.name }}"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Ensure apksigner is available
        run: |
          set -euo pipefail
          SDK="${ANDROID_HOME:-/usr/local/lib/android/sdk}"
          if ! ls "$SDK"/build-tools/*/apksigner >/dev/null 2>&1; then
            yes | "$SDK/cmdline-tools/latest/bin/sdkmanager" "build-tools;34.0.0" > /dev/null
          fi

      - name: Upload ${{ matrix.name }}.apk as its own artifact
        uses: actions/upload-artifact@v4
        with:
          name: apk-${{ matrix.name }}
          path: dist/${{ matrix.name }}.apk
          if-no-files-found: error
          retention-days: 7
          compression-level: 0

  release:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Collect all clone APKs (one artifact per clone)
        uses: actions/download-artifact@v4
        with:
          pattern: apk-*
          merge-multiple: true
          path: dist

      - name: Create release with one raw .apk asset per clone
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          ls -la dist
          test "$(ls dist/*.apk | wc -l)" -gt 0
          cd dist
          sha256sum *.apk > SHA256SUMS.txt
          cd ..
          TAG="clones-${{ github.run_number }}"
          gh release create "$TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --title "APK clones (run ${{ github.run_number }})" \
            --notes "One installable clone per name in names.txt.
          Each .apk is a separate raw asset - click any file below to download it directly (no zip wrapping).
          Package id: net.quetta.browser.<name> (lower-cased), app label: <name>.
          Verify your download against SHA256SUMS.txt before installing."
          gh release upload "$TAG" dist/*.apk dist/SHA256SUMS.txt --clobber --repo "$GITHUB_REPOSITORY"
